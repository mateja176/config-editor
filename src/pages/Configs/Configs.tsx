import { Container } from '@chakra-ui/react';
import React from 'react';

export interface ConfigsProps {}

const Configs: React.FC<ConfigsProps> = (props) => {
  return <Container>Configs</Container>;
};

export default Configs;
